import UIKit

func Alphabetize(_ newArray: [String])
{
    for element in newArray
    {
        print(element)
        
        if "a" == "A"
        {
            print(true)
        }
    }
}

var newArray: [String] = ["Apple", "Banana", "Carrot"]

Alphabetize(newArray)

